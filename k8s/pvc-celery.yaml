apiVersion: v1
kind: PersistentVolume
metadata:
  name: api-celery-pv
spec:
  capacity:
    storage: 1Gi 
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /var/pvc/cs-api-celery

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: api-celery-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
